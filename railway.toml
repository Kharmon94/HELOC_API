# Railway config for HELOC API (separate repo deploy)
# Root Directory: empty (repo root is the API)
# Railway auto-detects Dockerfile when present; no builder needed.

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
